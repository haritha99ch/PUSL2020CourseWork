<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="#">Accidents Reports</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="container">
        <div class="collapse navbar-collapse row" id="navbarNavDropdown">
            <ul class="navbar-nav col-11">
                <li class="nav-item active">
                    <a class="nav-link" href="@Url.Action("Index", "Home")">Home <span class="sr-only">(current)</span></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="@Url.Action("Index", "Report")">Reports</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="@Url.Action("Index", "Statistic")">Statistic</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="@Url.Action("AboutUs", "Home")">About Us</a>
                </li>
            </ul>

        </div>
    </div>
    <div class="navbar-nav col-1">
        <div class="nav-item active row">
            @if (Session["CurrentUserID"] == null) {
                <a class="nav-link col" href="@Url.Action("Index", "SignIn")">Sign In</a>
            }
            else {
                <a class="nav-link col" href="@Url.Action("SignOut", "SignIn")">SignOut</a>
                <a class="nav-link col" href="@Url.Action("Index", "Account")">
                    <img class="rounded-circle col" height="30" src="@Url.Content(Session["ProfilePicture"].ToString())" />
                </a>
            }
        </div>

    </div>
</nav>